
-- this is stored seperate from `player_data`
-- to allow better bungeecord support
-- and because why not lmao
--CREATE TABLE IF NOT EXISTS `player_ranks` (
--    `player_uuid` VARCHAR(100) NOT NULL,
--    `player_rank` SMALLINT NOT NULL DEFAULT 0,
----    0 = member, 20 = owner, 19 = admin
--)